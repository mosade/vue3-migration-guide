# 08.迁移全局指令注册

- 目标：迁移所有全局指令注册调用。
- 搜索线索：全局搜索 `Vue.directive(` 和集中注册指令的入口。
- 实施内容：改为 `app.directive(name, directive)`，并结合自定义指令钩子迁移任务更新 hook 名称。
- 注意事项：注册 API 和指令 hook 是两件事，只改注册方式仍可能在运行时失效。
- 完成标准：全局指令能注册，且在挂载、更新、卸载阶段行为正确。
