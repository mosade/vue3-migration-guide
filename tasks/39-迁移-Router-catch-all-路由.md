# 39.迁移 Router catch-all 路由

- 目标：修复 404 路由匹配语法差异。
- 搜索线索：全局搜索 `path: '*'`、`/*`、`404`、`NotFound`。
- 实施内容：将旧的 `*` 路由改为 `/:pathMatch(.*)*`，并确认放在路由表末尾。
- 注意事项：如果项目有权限重定向或动态路由，兜底路由的注册时机可能影响 404 和登录跳转。
- 完成标准：未知路径进入 404，已注册动态路由不会被兜底提前截获。
