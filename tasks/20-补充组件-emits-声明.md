# 20.补充组件 emits 声明

- 目标：为事件输出组件补充 Vue 3 事件声明。
- 搜索线索：全局搜索 `$emit(`、`emits:`、`defineEmits`、`@` 监听自定义组件。
- 实施内容：为组件声明所有对外事件；事件只是内部 DOM 监听时不要放进 `emits`；事件有 payload 约束时可以补 validator。
- 注意事项：Vue 3 会用 `emits` 判断事件是否应从 `$attrs` 中剔除，漏声明会导致事件可能被透传到根元素并重复触发。
- 完成标准：控制台没有未知事件/重复触发问题，父组件监听的自定义事件行为稳定。
