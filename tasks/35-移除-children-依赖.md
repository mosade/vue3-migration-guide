# 35.移除 $children 依赖

- 目标：移除对子组件实例树的隐式依赖。
- 搜索线索：全局搜索 `$children`、`this.$parent`、按下标访问子组件的逻辑。
- 实施内容：单个确定子组件用 `ref`；跨层级协作用 provide/inject；列表型子组件用数据驱动或显式注册/注销机制。
- 注意事项：不要用 DOM 查询替代 `$children`；`ref` 在 `v-for`、条件渲染、异步组件下的时机和结构都要单独验证。
- 完成标准：组件协作不依赖内部渲染顺序，增删子组件或调整模板结构后行为仍稳定。
