# 33.移除 $once 用法

- 目标：移除实例单次事件订阅 API。
- 搜索线索：全局搜索 `.$once(`、`$once(`。
- 实施内容：使用新 emitter 的 once 能力；如果没有 once，则用包装函数在第一次触发后主动取消订阅。
- 注意事项：一次性订阅也要处理组件提前卸载的清理，否则事件迟到时仍可能触发已销毁页面逻辑。
- 完成标准：事件最多触发一次，且组件卸载后不会再触发。
