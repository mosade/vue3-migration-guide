# 13.移除模板 filters

- 目标：移除 Vue 3 已删除的模板过滤器能力。
- 搜索线索：全局搜索模板中的 `|`，以及组件选项里的 `filters:`。
- 实施内容：纯格式化逻辑改为普通工具函数；依赖响应式状态的逻辑改为 computed；需要传参的过滤器改为显式函数调用。
- 注意事项：不要把表达式里的管道字符误判；全局 filter 要先落到统一工具模块，避免每个组件复制一份实现。
- 完成标准：模板中不再存在 Vue filter 语法，格式化结果与原页面一致。
