# 32.移除 $off 用法

- 目标：移除实例事件解绑 API。
- 搜索线索：全局搜索 `.$off(`、`$off(`，并对照同名 `$on`。
- 实施内容：使用新 emitter 的取消订阅 API；如果新 API 返回 unsubscribe 函数，统一在组件卸载时调用。
- 注意事项：匿名 handler 无法精准解绑，迁移时应抽成稳定函数引用或保存 unsubscribe。
- 完成标准：页面卸载、弹窗关闭、组件销毁后不再响应旧事件。
