# 25.迁移 v-if 与 v-for 同节点写法

- 目标：修复同节点 `v-if` 与 `v-for` 的优先级变化问题。
- 搜索线索：全局搜索同时包含 `v-if` 和 `v-for` 的模板节点。
- 实施内容：如果是过滤列表，优先改成计算属性；如果是整段条件渲染，将 `v-if` 放到外层容器或 `<template>`。
- 注意事项：Vue 3 中 `v-if` 优先级高于 `v-for`，原来依赖循环变量的条件可能会直接失效；不要为了快速修复引入额外无意义 DOM。
- 完成标准：空列表、部分过滤、全量展示三种状态与原业务预期一致。
