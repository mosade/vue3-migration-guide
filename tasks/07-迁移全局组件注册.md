# 07.迁移全局组件注册

- 目标：迁移所有全局组件注册调用。
- 搜索线索：全局搜索 `Vue.component(`、批量注册组件的 require/import 逻辑。
- 实施内容：改为在应用创建后调用 `app.component(name, component)`。
- 注意事项：自动批量注册如果依赖 webpack `require.context`，要确认构建链仍支持；组件名大小写和前缀规则不要顺手改动。
- 完成标准：原全局组件在模板中可继续解析，无 unknown component 警告。
