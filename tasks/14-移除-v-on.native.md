# 14.移除 v-on.native

- 目标：移除已废弃的 `.native` 监听方式。
- 搜索线索：全局搜索 `.native`。
- 实施内容：如果监听的是组件内部根元素事件，改为子组件显式 `$emit` 并声明 `emits`；如果只是透传原生事件，确认 `$attrs` 会落到正确元素。
- 注意事项：Vue 3 未声明到 `emits` 的监听可能作为原生监听透传到根节点，多根组件或包装组件必须明确事件落点。
- 完成标准：点击、输入、键盘等原交互仍触发一次且只触发一次。
