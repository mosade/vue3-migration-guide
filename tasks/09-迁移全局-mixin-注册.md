# 09.迁移全局 mixin 注册

- 目标：迁移全局 `mixin` 注册方式。
- 搜索线索：全局搜索 `Vue.mixin(`、全局混入文件、埋点/权限/字典类 mixin。
- 实施内容：改为 `app.mixin(mixin)`。
- 注意事项：全局 mixin 会影响所有组件，迁移后重点确认生命周期命名、实例属性访问和重复执行问题。
- 完成标准：全局混入仍生效，且没有引入全局重复请求、重复埋点或生命周期报错。
