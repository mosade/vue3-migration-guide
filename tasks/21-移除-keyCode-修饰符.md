# 21.移除 keyCode 修饰符

- 目标：移除旧版键盘编码修饰符写法。
- 搜索线索：全局搜索 `@keyup.`、`@keydown.`、`config.keyCodes`、数字键码如 `.13`、`.27`。
- 实施内容：将 keyCode 修饰符改为键名修饰符，例如 `.enter`、`.esc`、`.tab`，复杂组合改为事件处理函数内判断 `event.key`。
- 注意事项：不同浏览器/输入法下 `event.key` 可能有差异，快捷键和表单提交场景要实测。
- 完成标准：回车提交、Esc 关闭、方向键导航等键盘交互保持一致。
