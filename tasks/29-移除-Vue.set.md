# 29.移除 Vue.set

- 目标：删除 Vue 2 响应式补丁 API 之一。
- 搜索线索：全局搜索 `Vue.set`、`this.$set`。
- 实施内容：普通对象和数组优先改为直接赋值；需要触发不可变更新链路的地方用对象/数组替换。
- 注意事项：不要为了“保持响应式”保留多余包装；但 Vuex、表单缓存、深层对象合并处要确认引用变化是否被业务代码依赖。
- 完成标准：新增字段、数组项更新、嵌套对象更新都能驱动视图刷新。
