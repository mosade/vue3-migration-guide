# 24.迁移数组 watch 行为差异

- 目标：修正受 watch 语义变化影响的代码。
- 搜索线索：全局搜索 `watch:`、`$watch(`、`watch(`，重点看监听数组、对象列表、表格数据、树数据的逻辑。
- 实施内容：需要监听数组内部增删改时补 `deep: true` 或改为监听派生值；只关心整体替换时保持浅监听。
- 注意事项：不要默认给所有数组 watch 加 deep，大型列表会有性能风险；能通过明确事件、提交点或计算属性解决时优先减少深度监听。
- 完成标准：数组 push/splice、索引项属性修改、整体替换都按业务需要触发，且没有明显性能退化。
