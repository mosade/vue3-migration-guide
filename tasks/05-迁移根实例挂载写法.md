# 05.迁移根实例挂载写法

- 目标：完成根实例挂载 API 迁移。
- 搜索线索：全局搜索 `$mount(`、`el:`、`#app`。
- 实施内容：将 Vue 2 的 `el` 或 `$mount('#app')` 改为 `app.mount('#app')`。
- 注意事项：Vue 3 挂载不会替换挂载容器本身，依赖根 DOM 被替换的样式或脚本需要确认。
- 完成标准：根组件通过 `app.mount` 启动，首屏容器内能渲染应用内容。
