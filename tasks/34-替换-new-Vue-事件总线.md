# 34.替换 new Vue 事件总线

- 目标：移除基于 `new Vue()` 的事件总线实现。
- 搜索线索：全局搜索 `new Vue()`、`eventBus`、`bus.$on`、`bus.$off`、`bus.$emit`、`bus.$once`。
- 实施内容：改为独立 emitter；保留原事件名和 payload 结构，先替换总线实现，再逐步修调用方。
- 注意事项：组件卸载时必须取消订阅；`$once` 需要显式包装一次性监听；跨页面长期订阅容易产生重复触发和内存泄漏。
- 完成标准：事件发布、订阅、取消订阅、一次性订阅都可用，路由来回进入不会重复响应。
