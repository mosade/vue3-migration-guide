# 31.移除 $on 用法

- 目标：移除实例事件订阅 API。
- 搜索线索：全局搜索 `.$on(`、`$on(`。
- 实施内容：组件间通信改为 props/emits、store 或独立 emitter；保留事件名和 payload 结构，减少一次性改动范围。
- 注意事项：迁移时必须同时处理解绑，否则旧页面反复进入会重复订阅。
- 完成标准：订阅逻辑不再依赖 Vue 实例事件系统，重复进入页面不会重复触发。
