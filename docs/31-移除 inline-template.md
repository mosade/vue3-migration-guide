# 31. 移除 inline-template

## 当前任务

删除 `inline-template`。

## 怎么做

1. 搜索 `inline-template`。
2. 如果是老的内联模板组件，改成正常组件模板、slot 或模板文件。
3. 不要继续保留 `inline-template` 属性。

## 改完以后确认

- 代码里不再出现 `inline-template`。
