# 23. 修正 transition 作为根节点的写法

## 当前任务

处理 `<transition>` 作为组件根节点时的行为变化。

## 怎么做

1. 搜索根节点就是 `<transition>` 的组件。
2. 如果父组件通过 `v-if` 直接控制这个组件显示隐藏，不要再依赖外层切换触发过渡。
3. 改成给组件传一个 `show` 之类的布尔值，在组件内部真正包裹的元素上使用 `v-if`。

## 改完以后确认

- 这类组件的显示隐藏过渡仍然生效。
