# 07. 调整 v-bind object 的覆盖顺序

## 当前任务

修正 `v-bind="object"` 在 Vue 3 中受书写顺序影响的问题。

## 怎么做

1. 搜索同时使用 `v-bind="obj"` 和显式属性（如 `id`、`class`、`type`、`value`）的节点。
2. 判断最终到底要让哪一个值生效。
3. 通过调整属性书写顺序来保证最终覆盖结果正确。
4. 不要默认认为显式属性一定覆盖 `v-bind`。

## 改完以后确认

- 每个混合绑定节点都已经按预期顺序排列。
