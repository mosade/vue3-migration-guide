# 08. 移除 .native 事件修饰符

## 当前任务

移除组件监听里的 `.native`。

## 怎么做

1. 全局搜索 `.native`。
2. 把 `@click.native` 这类写法改成普通的 `@click`。
3. 检查子组件是否真的通过组件事件对外暴露了这个事件；如果没有，就在子组件里补 `emits`，或者让父组件直接监听真实 DOM 事件。

## 改完以后确认

- 代码里不再出现 `.native`。
