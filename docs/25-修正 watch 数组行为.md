# 25. 修正 watch 数组行为

## 当前任务

修正数组 watch 在 Vue 3 中默认只监听替换、不监听变异的问题。

## 怎么做

1. 搜索监听数组的 `watch` 配置或 `watch(...)` 调用。
2. 如果业务依赖 `push`、`splice`、`sort` 这类变异也触发回调，就补上 `deep`。
3. 只给真正需要的数组监听补 `deep`，不要全局乱加。

## 改完以后确认

- 数组变异后，依赖 watch 的逻辑还能正常触发。
