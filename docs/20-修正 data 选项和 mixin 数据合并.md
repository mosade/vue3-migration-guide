# 20. 修正 data 选项和 mixin 数据合并

## 当前任务

处理 `data` 必须是函数，以及 mixin / extends 数据不再深合并的问题。

## 怎么做

1. 搜索 `data: {`。
2. 把对象形式的 `data` 改成函数返回对象。
3. 检查 mixin 和组件本身是否都定义了相同的嵌套对象字段。
4. 如果以前依赖深合并，改成显式 merge，不要再依赖框架自动合并。

## 改完以后确认

- 所有组件 `data` 都是函数。
- 依赖深合并的地方已经手动处理。
