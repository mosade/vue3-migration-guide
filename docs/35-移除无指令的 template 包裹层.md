# 35. 移除无指令的 template 包裹层

## 当前任务

处理没有特殊指令的 `<template>` 在 Vue 3 中会真实渲染出来的问题。

## 怎么做

1. 搜索不带 `v-if`、`v-for`、`v-slot` 的普通 `<template>`。
2. 如果它只是无意义包裹层，直接删掉。
3. 如果确实需要结构包裹，改成真实元素。

## 改完以后确认

- 代码里不再依赖普通 `<template>` 只渲染内容、不渲染标签的旧行为。
