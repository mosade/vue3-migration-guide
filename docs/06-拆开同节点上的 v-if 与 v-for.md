# 06. 拆开同节点上的 v-if 与 v-for

## 当前任务

避免在同一个元素上同时使用 `v-if` 和 `v-for`。

## 怎么做

1. 全局搜索同时带有 `v-if` 和 `v-for` 的节点。
2. 优先把过滤逻辑提到计算属性或方法里，先得到一个已经过滤好的列表。
3. 如果确实需要条件包裹，给当前节点加一层父节点，把 `v-if` 和 `v-for` 分开。

## 改完以后确认

- 同一个元素上不再同时出现 `v-if` 和 `v-for`。
