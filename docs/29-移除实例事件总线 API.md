# 29. 移除实例事件总线 API

## 当前任务

去掉 `$on`、`$off`、`$once` 这套实例事件总线 API。

## 怎么做

1. 搜索 `$on(`、`$off(`、`$once(`、`new Vue()` 事件总线。
2. 如果只是父子组件通信，改成 props + emits。
3. 如果是跨层级共享，改成 provide/inject 或 store。
4. 如果确实需要事件总线，换成独立事件库，不要继续依赖 Vue 实例。

## 改完以后确认

- 项目里不再通过 Vue 实例做事件总线。
