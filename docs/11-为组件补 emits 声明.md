# 11. 为组件补 emits 声明

## 当前任务

把组件对外发出的事件显式声明出来。

## 怎么做

1. 搜索组件里的 `$emit(`。
2. 逐个统计组件实际会发出的事件名。
3. 在组件选项里补 `emits: []` 或 `emits: {}`。
4. 如果组件在重复转发原生事件，例如 `$emit('click')`，要明确决定是保留组件事件还是直接让父组件监听 DOM。

## 改完以后确认

- 所有有 `$emit` 的组件都已经声明 `emits`。
